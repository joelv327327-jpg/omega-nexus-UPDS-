# Directrices de ramas

Al menos una rama adicional (por ejemplo, `feature/estructura-html`) fusionada posteriormente a la rama principal (`main`).

Notas:
- Crear ramas para features y fixes.
- Abrir un Pull Request y solicitar revisión antes de fusionar.
- Usar nombres descriptivos como `feature/descripcion` o `fix/descripcion`.
